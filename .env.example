DATABASE_URL=postgres://user:pass@host/db
AUTH_SECRET=                   # >= 32 chars; openssl rand -hex 32
YT_BASE_URL=https://example.youtrack.cloud
# Auth is via YouTrack Personal Access Token — users paste their PAT in the
# login form (Profile → Account Security → New token). No env var holds the PAT.
YT_TOKEN_ENC_KEY=              # base64-encoded 32 bytes; openssl rand -base64 32
# Optional — leave blank to auto-discover from the sprint's issues at session create.
# These act as fallback values used only when discovery returns nothing.
YT_SP_FIELD=
YT_DURATION_FIELD=
YT_DONE_STATE_NAMES=
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=
NEXT_PUBLIC_PUSHER_KEY=
NEXT_PUBLIC_PUSHER_CLUSTER=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
